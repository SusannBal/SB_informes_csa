# Copiá este archivo a ".env". La pantalla de acceso (usuario y contraseña de
# prueba, mientras no existe el login definitivo) compara contra estos dos
# valores directamente — no hace falta ningún servicio externo, todo pasa en
# el navegador.

VITE_APP_USER=
VITE_APP_PASSWORD=
