# Copiá este archivo a ".env". La pantalla de acceso (una única contraseña
# de prueba, mientras no existe el login definitivo) compara contra esta
# huella — no hace falta ningún servicio externo, todo pasa en el navegador.
#
# Para generarla:  node scripts/hashearClave.mjs "la-contraseña-que-elijas"

VITE_APP_PASSWORD_HASH=
